# use this to load accelerometer library and set up device in python shell

# use thusly:
# import test as a

# or use a differnet name.  One letter is suggested as the point is to manipulate individual functions and names easily in the shell.

from accelerometer import *
SetDevice()

